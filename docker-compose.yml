version: "3.9"

services:
  task-manager-api:
    build: .
    container_name: task_manager_api
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/code/data   # persist the SQLite file on the host
    restart: unless-stopped
